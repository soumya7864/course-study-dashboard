const NotFoundPage = () => {
  return (
    <div
      className="centered"
      style={{ fontSize: 26, color: "red", fontWeight: "bold" }}
    >
      <p>Page not found!</p>
    </div>
  );
};

export default NotFoundPage;
